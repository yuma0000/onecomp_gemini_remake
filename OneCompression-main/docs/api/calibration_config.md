# CalibrationConfig

The `CalibrationConfig` dataclass groups all calibration-related parameters
used by `Runner` and `AutoBitQuantizer`.

::: onecomp.calibration.CalibrationConfig
    options:
      show_source: false
      heading_level: 2
