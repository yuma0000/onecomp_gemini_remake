pages:
  image: python:3.11
  stage: deploy
  script:
    - pip install mkdocs mkdocs-material mkdocstrings mkdocstrings-python
    - mkdocs build --site-dir public
  artifacts:
    paths:
      - public
  when: manual
